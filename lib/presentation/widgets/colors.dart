import 'package:flutter/material.dart';

class AppColors{

static const Color background= Color(0xFFF9D600);
static const Color blueaccent=Color(0xFF0088F0);
static const Color bitblack = Color(0xFF7C7C7C);
  static final Color scbg = Colors.grey.shade200; // changed to `final` as `shade200` is a runtime value
  static  const Color white= Colors.white;
  






}